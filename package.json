{
  "name": "theatre",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter client dev",
    "dev:server": "pnpm --filter server dev",
    "dev:all": "pnpm --parallel --filter client --filter server dev",
    "build": "pnpm --filter rules-engine build && pnpm --filter client build && pnpm --filter server build",
    "test": "pnpm --filter rules-engine test"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
